# Tells `shopify app dev` how to start this app's server. The CLI passes in
# PORT, SHOPIFY_API_KEY, SHOPIFY_API_SECRET, SHOPIFY_APP_URL and SCOPES, and
# points its HTTPS tunnel at this process.
name = "web"
roles = ["frontend", "backend"]

[commands]
dev = "npm run dev"
